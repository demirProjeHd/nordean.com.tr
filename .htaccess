<IfModule mod_rewrite.c>
    RewriteEngine On

    # Don't rewrite if file/directory exists
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Forward all requests to public folder
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
